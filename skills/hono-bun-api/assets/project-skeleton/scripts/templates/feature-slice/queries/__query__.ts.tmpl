import { success, failure, type Result } from '@shared/result';
import { notFound, type AppError } from '@shared/errors/app-error';
import type { __Features__ReadContext } from '../read-context';
import type { __Feature__Dto } from '../schemas';

// TODO: importar tu tabla de @shared/db/schema y usar drizzle eq()
// import { eq } from 'drizzle-orm';
// import { __features__ } from '@shared/db/schema';

/**
 * Query: lee directo de la DB usando ctx.db. No usa el repo.
 */
export const get__Feature__ByIdHandler = async (
  ctx: __Features__ReadContext,
  { id }: { id: string },
): Promise<Result<__Feature__Dto, AppError>> => {
  // const rows = await ctx.db
  //   .select()
  //   .from(__features__)
  //   .where(eq(__features__.id, id))
  //   .limit(1);
  // const row = rows[0];
  // if (!row) return failure(notFound('__Feature__', id));
  // return success(toDto(row));

  // Stub mientras no esté la tabla real:
  ctx.logger.warn({ id }, 'get__Feature__ById: stub — implement con tabla real');
  return failure(notFound('__Feature__', id));
};
