export type PolicyDecision = { allowed: true } | { allowed: false; reason: string };

export const canUpdate__Feature__ = (actorId: string | null): PolicyDecision => {
  if (!actorId) return { allowed: false, reason: 'authentication required' };
  return { allowed: true };
};
